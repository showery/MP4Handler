APP_ABI := armeabi-v7a

APP_STL := gnustl_static

APP_CPPFLAGS := -frtti

APP_MODULES += mp4v2
APP_MODULES += render
APP_MODULES += codec
APP_MODULES += mediasdk
APP_MODULES += interface
APP_MODULES += codec16
APP_MODULES += codec17
APP_MODULES += codec18
APP_MODULES += codec19
